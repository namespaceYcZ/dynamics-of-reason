// 以下为人名
#let michael_friedman() = underline(text(style: "italic", font: "KaiTi")[迈克尔·弗里德曼]);
#let friedman() = underline(text(style: "italic", font: "KaiTi")[弗里德曼]);
#let immanuel_kant() = underline(text(style: "italic", font: "KaiTi")[伊曼努尔·康德]);
#let kant() = underline(text(style: "italic", font: "KaiTi")[康德]);
#let hans_reichenbach() = underline(text(style: "italic", font: "KaiTi")[汉斯·赖欣巴赫]);
#let reichenbach() = underline(text(style: "italic", font: "KaiTi")[赖欣巴赫]);
#let carl_gustav_hempel_complex() = underline(text(
  style: "italic",
  font: "KaiTi",
)[卡尔·古斯塔夫（"彼得"）·亨佩尔]);
#let carl_gustav_hempel() = underline(text(
  style: "italic",
  font: "KaiTi",
)[卡尔·古斯塔夫·亨佩尔]);
#let hempel() = underline(text(
  style: "italic",
  font: "KaiTi",
)[亨佩尔]);
#let rudolf_carnap() = underline(text(style: "italic", font: "KaiTi")[鲁道夫·卡尔纳普]);
#let carnap() = underline(text(style: "italic", font: "KaiTi")[卡尔纳普]);
#let thomas_kuhn() = underline(text(style: "italic", font: "KaiTi")[托马斯·库恩]);
#let kuhn() = underline(text(style: "italic", font: "KaiTi")[库恩]);
#let w_v_quine_complex() = underline(text(style: "italic", font: "KaiTi")[威拉德·范·奥曼·奎因]);
#let w_v_quine() = underline(text(style: "italic", font: "KaiTi")[W. V. 奎因]);
#let quine() = underline(text(style: "italic", font: "KaiTi")[奎因]);
#let otto_neurath() = underline(text(style: "italic", font: "KaiTi")[奥托·纽拉特]);
#let neurath() = underline(text(style: "italic", font: "KaiTi")[纽拉特]);
#let plato() = underline(text(style: "italic", font: "KaiTi")[柏拉图]);
#let aristotle() = underline(text(style: "italic", font: "KaiTi")[亚里士多德]);
#let descartes() = underline(text(style: "italic", font: "KaiTi")[笛卡尔]);
#let leibniz() = underline(text(style: "italic", font: "KaiTi")[莱布尼茨]);
#let newton() = underline(text(style: "italic", font: "KaiTi")[牛顿]);
#let albert_einstein() = underline(text(style: "italic", font: "KaiTi")[阿尔伯特·爱因斯坦]);
#let einstein() = underline(text(style: "italic", font: "KaiTi")[爱因斯坦]);
#let hermann_von_helmholtz() = underline(text(
  style: "italic",
  font: "KaiTi",
)[赫尔曼·冯·亥姆霍兹]);
#let helmholtz() = underline(text(style: "italic", font: "KaiTi")[亥姆霍兹]);
#let ernst_mach() = underline(text(style: "italic", font: "KaiTi")[恩斯特·马赫]);
#let mach() = underline(text(style: "italic", font: "KaiTi")[马赫]);
#let henri_poincare() = underline(text(style: "italic", font: "KaiTi")[亨利·庞加莱]);
#let poincare() = underline(text(style: "italic", font: "KaiTi")[庞加莱]);
#let moritz_schlick() = underline(text(style: "italic", font: "KaiTi")[莫里茨·石里克]);
#let schlick() = underline(text(style: "italic", font: "KaiTi")[石里克]);
#let ludwig_wittgenstein() = underline(text(style: "italic", font: "KaiTi")[路德维希·维特根斯坦]);
#let wittgenstein() = underline(text(style: "italic", font: "KaiTi")[维特根斯坦]);
// 以下为其他专有名词
#let dynamics_of_reason() = text(style: "italic", font: "SimHei")[理性的动态性];
#let logical_empiricism() = text(style: "italic", font: "SimHei")[逻辑经验主义];
#let logical_positivism() = text(style: "italic", font: "SimHei")[逻辑实证主义];
#let logical_positivist() = text(style: "italic", font: "SimHei")[逻辑实证主义];
#let logical_positivist2() = text(style: "italic", font: "SimHei")[逻辑实证主义的];
#let logical_empiricists() = text(style: "italic", font: "SimHei")[逻辑经验主义者];
#let kantian() = text(style: "italic", font: "SimHei")[康德主义];
#let neo_kantian() = text(style: "italic", font: "SimHei")[新康德主义];
#let newtonian() = text(style: "italic", font: "SimHei")[牛顿式的];
// 超越（transcendence）在哲学中传达的是源自该词（拉丁语）字面意思的基本概念，即越过。它是指处于普通经验或理解之外的状态，涉及超越知觉和知识的通常界限，访问更高或更深层次的现实，并且侧重于理性、意识或现实的本质等概念, 试图提供一个不依赖于宗教信仰或超自然力量的理解世界的框架，也是一个关于存在的知识的显现和验证的框架。
#let transcendence() = text(style: "italic", font: "SimHei")[超越];
// 对于康德而言，“超越的”(transcendent)的意思是指那些超越我们知识能力的范畴，与“超验的”（transcendental）相对。
#let transcendent() = text(style: "italic", font: "SimHei")[超越的];
#let transcendental() = text(style: "italic", font: "SimHei")[超验的];
#let transcendental_logic() = text(style: "italic", font: "SimHei")[超验哲学];
#let a_priori() = text(style: "italic", font: "SimHei")[先验的];
#let a_priori_n() = text(style: "italic", font: "SimHei")[先验];
#let tractatus_logico_philosophicus() = text(style: "italic", font: "SimHei")[《逻辑哲学论》];
#let internal_questions() = text(style: "italic", font: "SimHei")[内部问题];
#let external_questions() = text(style: "italic", font: "SimHei")[外部问题];
