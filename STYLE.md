# 排版与译名体例

本文件记录本项目统一采用的标点、格式与译名体例，供后续增补内容时参照。
可运行 `python3 tools/check_style.py` 自动检查大部分条目。

## 1. 括号

- 中文正文中的括号一律使用**全角** `（）`。
  - 术语后附英文原文：`*协调原则*（_principles of coordination_）`
  - 人名后附外文原名：`*#schelling()*（Friedrich Schelling）`
- **括号嵌套**统一使用全角 `（）`，不混用 `[]` 或 `〔〕`：
  - `（_relative spaces_，即*参考系*（_reference frames_））`
- 半角 `()` 只用于三种情形：
  1. 纯西文的文献引证，如 `Schilpp (1949)`、`Kuhn (1962/70, pp. 5, 7)`；
  2. 数学公式内部；
  3. Typst 语法本身。
- `[]` 只用于**转写原书在英文引文中所加的编辑性插入**，如
  `（_constitutive of the concept of the object of [scientific] knowledge_）`。
- `〔〕` 只用于**译者在译出的引文内部所加的补足语**，如
  `“如果〔#hume()〕把我们的问题以其〔充分的〕一般性来考虑……”`。

## 2. 引号

- 中文引号用 `“”`；引号内再引用时用 `‘’`。
- 不使用 ASCII 的 `"` 与 `'`（Typst 字符串字面量、数学表达式内部除外）。

## 3. 破折号与连接号

- 中文破折号为**双字符** `——`。
- 连接两个专名或两个概念时用单个 `—`，如 `#leibniz()—#clarke()通信`、`位置—动量`。
- 数字区间（页码、年份、世纪）一律用连接号 `–`：`pp. 245–6`、`1889–1904`、`20世纪20–40年代`。
- 省略号用 `……`（英文引文内部保留原文的 `...`）。

## 4. 其他标点

- 中文语境中的冒号、分号、逗号、顿号一律全角：`：；，、`。
- 全角标点前后不加空格；`Friedman（1999, 第九章）` 而非 `Friedman （1999, 第九章）`。
- `#orig-note[...]` 的条目以句号 `。` 收尾。

## 5. 强调标记

- 加粗一律用 `*…*`，斜体一律用 `_…_`；不使用 `#strong[...]` / `#emph[...]`。
- 西文术语、书刊名以 `_…_` 排斜体；中文书名用 `《》`。

## 6. 译名与词表

- 反复出现的人名、需要固定格式的术语，统一在 `glossary.typ` 中定义，并在正文中以
  函数调用的形式使用（如 `#kant()`、`#relativized_a_priori()`），不要直接写汉字。
- 首次出现时在其后以全角括号附外文原名。
- 词表函数在下列位置**不**使用，以免破坏排版或语法：
  - 各级标题；
  - `《》` 内的书名；
  - Typst 字符串字面量 `"…"`、数学公式与代码块内部。
- 已知的固定译法：Allison = 阿利森（不作“艾利森”）、Duhem = 杜恒、Helmholtz = 亥姆霍兹、
  Poincaré = 庞加莱、Schlick = 石里克、Neurath = 纽拉特、Reichenbach = 赖欣巴赫。

## 7. 译注

- 译注面向无哲学背景的读者，以**补足背景**为目的，避免复述正文已有的内容。
- 不写"让我们……""下面逐一展开""准备好后请发……"一类的对话式或提纲式套话。
- 同一背景知识只在首次需要处详注，其后以"参见前文注X"或 `#ref-link` 交叉引用。
