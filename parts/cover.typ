#import "../glossary.typ": *   // 导入所有定义
#import "../common.typ": *   // 导入所有定义
#page(header: none, footer: none)[
  #align(center)[
    #v(4cm)
    #text(font: "SIMHEI", size: 38pt, weight: "bold")[#dynamics_of_reason()]
    #v(1cm)
    #text(font: "SIMHEI", size: 30pt, weight: "bold")[#michael_friedman()]
  ]
]

#pagebreak(weak: true)
