#import "../glossary.typ": *   // 导入所有定义
#import "../common.typ": *   // 导入所有定义

#align(center)[
  #set text(font: "SIMHEI", size: 38pt, weight: "bold")
  #dynamics_of_reason()
]
#align(center)[
  #set text(font: "SIMHEI", size: 30pt, weight: "bold")
  #michael_friedman()
]
#pagebreak(weak: true)
